[package]
name = "typesafe-jev-sdk"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features=["full"] }
serde = { version = "1", features=["derive"]}
reqwest = { version = "0.13", features=["json"]}


[dev-dependencies]
serde_json = "1"